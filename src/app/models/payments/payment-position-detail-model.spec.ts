import { PaymentPositionDetailModel } from './payment-position-detail-model';

describe('PaymentPositionDetailModel', () => {
  it('should create an instance', () => {
    expect(new PaymentPositionDetailModel()).toBeTruthy();
  });
});
