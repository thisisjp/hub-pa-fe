import { PublishExportModel } from './publish-export-model';

describe('PublishExportModel', () => {
  it('should create an instance', () => {
    expect(new PublishExportModel()).toBeTruthy();
  });
});
