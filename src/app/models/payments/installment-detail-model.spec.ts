import { InstallmentDetailModel } from './installment-detail-model';

describe('InstallmentDetailModel', () => {
  it('should create an instance', () => {
    expect(new InstallmentDetailModel()).toBeTruthy();
  });
});
