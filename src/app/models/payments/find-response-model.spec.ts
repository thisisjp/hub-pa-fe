import { FindResponseModel } from './find-response-model';

describe('FindResponseModel', () => {
  it('should create an instance', () => {
    expect(new FindResponseModel()).toBeTruthy();
  });
});
