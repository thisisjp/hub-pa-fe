import { UserIntrospectResponse } from './user-introspect-response';

describe('UserIntrospectResponse', () => {
  it('should create an instance', () => {
    expect(new UserIntrospectResponse()).toBeTruthy();
  });
});
